{
  "private": true,
  "dependencies": {},
  "scripts": {
    "lint:check": "yarn workspaces run lint:check",
    "lint:write": "yarn workspaces run lint:write",
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "coverage": "yarn workspaces run coverage"
  },
  "devDependencies": {},
  "workspaces": [
    "core"
  ]
}
